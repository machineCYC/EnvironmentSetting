增加新的repositories 
1. $ cd /d/git
2. 在github先新增一個repositories
3. $ git clone HTTP
4. $ git init

創建空文件
$ touch 檔名.py

指定到特定資料夾
$ cd /d/git/GitTUT
$ cd /d/git/Evolutionary-Algorithms
$ cd /d/git/Reinforcement-Learning
$ cd /d/git/EnvironmentSetting
$ cd /d/git/Face-Recognition
$ cd /d/git/Hands-on-Machine-Learning-with-Scikit-Learn-and-TensorFlow
$ cd /d/git/2017MLSpring_Hung-yi-Lee
$ cd /d/git/Coursera
$ cd /d/git/SideProjects

開新分支
git branch branch_name

切換分支
git checkout branch_name

建立 local 分支且 push 上 remote
git checkout -t -b local_branch_name origin local_branch_name

push 上 remote 這分之，不存在則 remote 端會建立
git push origin local_branch_name

與 remote 同步
git remote update

修改或合併 commit (但之後 local branch 會跟 remote 不一樣，所有 push 記得要加 -f) 
git rebase -i commit_id

增加檔案
$ git add 檔案名稱.py

說明更改項目
$ git commit -m "....."?

上傳到github
$ git push -u origin master

更改A.py檔名為B.py
$ git mv A.py B.py

將A.txt搬移到my_folder資料夾下
$ git mv A.txt my_folder/

回上一層	
$ cd ..

刪除檔案
$ git rm 檔名.py
$ git commit -m "....."?

查看狀況
$ git status

列出所在資料夾的檔案	
$ ls

常看使用者姓名和email
$ git config user.name
$ git config user.email

查看過去的提交紀錄
$ git log